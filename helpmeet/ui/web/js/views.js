/**
 * Renderizado de vistas (~1,500 lineas).
 *
 * Cada vista (home, meeting, recording, settings) tiene su
 * propia funcion de renderizado que construye el DOM.
 *
 * @module js/views
 */

export { renderHome } from './views/home.js';
export { renderMeeting } from './views/meeting.js';
export { renderSettings } from './views/settings.js';
